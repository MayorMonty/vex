import { h, Component } from "preact";

/**
 * Provides standardized loading and error messages across the app, and allows for render functions to be async
 */
export default class AsyncComponent extends Component {}
